# 框架版本历史留底

> 本文件保存根入口文档曾经内嵌的框架版本历史。根入口文档只保留当前工作规则和少量导航，详细变更记录迁移到这里留底。

## 版本历史

- 2026-07-20：`/withGo` 目标理解与子目标拆解门禁过渡版 — 新增 `WITH-Go.md`，触发词 `/withGo`、`/withGoal`、`withGo`、`withGoal`。目标体系暂不改为真实树形目录或新数据结构，只把旧“当前分点”语义升级为“当前子目标”，保留原序号规则；子目标下允许 Agent 共同维护 `总览`、`分点`、`检查项`、`待确认`。同步更新 `🎛️_指令总览.md`、`_WITH-系列说明.md`、`AS-TL.md`、根入口文档和 `目标总览-模版.md`；当前 `目标总览.md` 已将标题过渡为 `当前子目标/挂起子目标`，并重点整理 GOAL-102 开票计划编辑、汇总行展示名称、到票登记与状态金额回传等子目标。动机：在不提前实现树形目标模型的前提下，让目标开始前先完成目标理解和可执行分点拆解，降低 `/withPlan` 和实施阶段的上下文缺口
- 2026-07-17：SQL 历史文件不回改 + withPlan 项目视角强化 — `AS-TL.md` 升级 v2.9，新增 TL SQL 修改边界：不直接修改本轮对话以外的既有 SQL，历史 SQL 默认视为已人工执行过一次，修正通过本轮补丁 SQL 承载，例外仅限人类明确允许或本轮目标为 SQL 合并；`UTIL-SQL管理规范.md` 新增「历史 SQL 修改边界」强制规则和反模式，并要求新建表和涉及字符集/排序规则的 DDL 默认使用 `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci`；`WITH-Plan.md` 升级 v1.1，要求计划从项目交付视角说明页面入口、按钮、筛选项、弹窗、列表列、后端接口、权限菜单 SQL、表字段、字典/枚举、DO/VO/DTO、接口入参出参等影响；同步更新 `配置与元数据/产物工件规范.md` 和根入口摘要。动机：SQL 在人工控制下只执行一次，避免回改历史脚本造成仓库内容与已执行数据库状态不一致；计划产物要让人类从项目功能和数据结构角度审查，而不是只看到技术文件列表；字符集和排序规则统一为 `utf8mb4_general_ci`，减少跨表字符比较和中文内容存储差异
- 2026-07-14：数据结构字段分组可审核性 — `UTIL-SQL管理规范.md` 新增「DDL 字段分组注释」强制规则：SQL DDL 按关联字段、业务字段、基础展示字段、系统管理字段分组，复杂业务字段可继续细分；`AS-TL.md` 升级 v2.7，要求 TL 设计 SQL、DO、数据库交互 VO 时维护字段分组和顺序；`CHK-项目代码质量自检` 升级 v2.3，新增维度 7「数据结构字段分组与可审核性」；`WITH-Check.md` 升级 v1.1，DDL/DO/Entity/数据库投影 VO 变更自动触发维度 7 检查。动机：让人类快速区分关联字段、业务字段、系统管理字段和基础字段，同时让 AI 后续追加字段时把同类字段整理得更靠近、顺序更合理
- 2026-07-14：根入口维护规则与历史迁移 — `AS-Teacher.md` 升级 v2.1，新增根入口维护规则：一切根入口说明文档优先修改 `AGENTS.md`，`agent.md`、`CLAUDE.md` 等同类入口直接复制同步，不单独生成。根入口长篇版本历史迁移到 `A.Teacher分析/框架版本历史.md` 留底，根入口仅保留短指针和当前规则。动机：减少入口文档漂移，降低启动上下文噪音，让根入口专注于当前可执行规则
- 2026-07-14：AS-TL 交付总结结构化 — `AS-TL.md` 升级 v2.6，强化「代码修改后汇报」为固定交付总结：SQL 文件位置与用途、前后端改动概览（功能/按钮位置/弹窗/列表等用户可见入口）、`/** IMPORTANT */` 标注逻辑说明、数据结构变动说明、人工复查事项、验证边界和构建命令声明。动机：让人类在验收时快速定位 SQL、页面变化、关键业务逻辑和数据契约影响，不需要从文件列表反推交付面
- 2026-07-10：AS-TL 代码修改后汇报强化 — `AS-TL.md` 升级 v2.5，新增「代码修改后汇报」强制规则：每次项目代码修改完成后，最终回复必须说明改动内容、人工复查事项、验证边界，并默认声明未运行 `mvn`/`pnpm`/`npm`/`yarn`。同时将 Maven 与前端包管理/构建命令限制从"不主动跑构建"强化为"未经本轮明确要求，不运行 `mvn`/`pnpm`/`npm`/`yarn` 及 compile/test/build/lint/typecheck/install 等子命令"。动机：减少耗时/环境敏感命令误触发，让人类复查有明确抓手
- 2026-07-08：TL 重心微调 — TL 写完代码后不主动运行 `mvn`/`pnpm`/`npm`/`yarn` 等构建命令（除非用户明确要求），更新 AS-TL.md「能做的一切」+ CLAUDE.md TL 模式速查。SQL 生成规范新增「反向SQL」分区：所有生成的 SQL 文件末尾附带注释掉的回滚 SQL（DELETE/ALTER DROP），供人工手动回滚测试。更新 UTIL-SQL管理规范.md 文件内容组织模板 + 4 个 SQL 模板 SKILL.md（menu/dict/perm/ecd）使用流程。动机：构建命令耗时且常需人工确认参数，Agent 不应自动触发；回滚 SQL 注释化降低手动回滚门槛
- 2026-07-07：代码注释规范与人工可审核性 — 新建 `通用能力层/工具手册/UTIL-代码注释规范.md`（三级注释层级：标准 JavaDoc → 全流程注释 → 类级注释；重要方法判定规则 R1-R8；`/** IMPORTANT */` / `/** IMPORTANT TODO */` 标记机制）。新建 `/withAnnotate` 指令（`WITH-Annotate.md`，扫描 IMPORTANT TODO → 生成全流程注释 → 穿透分析依赖方法 → 标记转换 → 零残留目标）。`CHK-项目代码质量自检` 升级 v2.2：新增维度 6「代码注释与可审核性」（公共接口 JavaDoc / 重要方法全流程注释 / IMPORTANT 标记一致 / 无 IMPORTANT TODO 残留）。`AS-TL.md` 升级 v2.3：代码质量规范新增「代码注释能力」章节（自动判定+注释生成+响应 IMPORTANT TODO）。更新 `WITH-Check.md` 维度判定规则、`_WITH-系列说明.md` 可用指令表、`🎛️_指令总览.md`（指令全表/触发检测/文件索引/场景）、CLAUDE.md（指令索引/决策树/导航策略/版本历史）。动机：AI 自动检查（withCheck）覆盖代码模式规范，业务逻辑正确性必须人工审核——全流程注释让审核者无需追踪调用链即可理解代码全貌
- 2026-07-03：SQL 管理规范化 — 新建 `通用能力层/skills/sql-templates/` 框架级 SQL 模板库（menu/dict/ecd 从 fms-rear 提升为跨项目共享，新建 `perm-template` 填补 role-menu 权限关联 SQL 空白）。新建 `通用能力层/工具手册/UTIL-SQL管理规范.md`（一功能一文件、SQL 类型与模板映射、文件命名规则、文件内容组织分区、Agent 生成协议、反模式）。修正 `配置与元数据/项目注册表.yaml`：lasen-rear 移除不存在的 `sql_dir`（SQL 散落各模块，待后续统一）。更新 `🛠️_能力总览.md`（新增 skills 行）、CLAUDE.md（导航表+版本历史）。动机：权限/字典 SQL 需与 DDL 统一管理，避免手工后台配置丢失
- 2026-07-03：目标总览模版化 — 新建 `A.目标体系/目标总览-模版.md`（独立模版文件，含完整字段说明、多维度状态标签参考、AI 读取协议、FW-FrameworkInit 初始化行为定义）。`FW-FrameworkInit` 升级 v1.2：Phase 0 新增目标总览初始化步骤（首次 clone 后自动从模版创建 `目标总览.md`）。解决新用户初始化后 AI 不理解目标文件格式的问题。更新 CLAUDE.md 导航策略表（理解工作方向行增加模版文件引用）
- 2026-07-01：检查清单 v2.1 维度1 强化数据访问性能检查 — `CHK-项目代码质量自检` 维度1 从"可读性与工具使用"升级为"数据访问性能与规范"：1.3 从仅检查 N+1 查询扩展为检查循环内所有数据库访问（`selectById`/`updateById`/`save`/`insert`）；新增 1.5「更新字段最小化」（禁止 `updateById(完整DO)` 更新单列，应使用 `update(entity, LambdaUpdateWrapper)` 只 SET 变更列，避免并发覆盖+写放大）。更新 `WITH-Check.md` 触发条件描述（数据库查询→数据库访问）。动机：GOAL-103 `updateInvoiceName` 逐条 `updateById` + 全字段更新 → BFS收集ID + `update(entity, wrapper)` 优化的实战教训
- 2026-07-01：`/withCheck` 纯代码规范检查 + 检查清单 v2.0 — 新建 `WITH-Check.md`（TL描述改动→Agent判定维度→派发QA-004纯代码检查→报告→TL修复，不检查业务）。`CHK-项目代码质量自检` 升级 v2.0：新增第5维度「导入/导出/上传等特殊功能」（6项检查：EasyExcel链式setter/导入资源关闭/导出VO列匹配/导出大数据量/上传文件校验/文件路径安全）。`/withFinish` 步骤2改为委托 `/withCheck` 执行。更新 AS-TL.md（决策树+代码质量章节）、`🎛️_指令总览.md`、`_WITH-系列说明.md`、CLAUDE.md（指令索引/决策树/导航）。新建 `KB-Lombok-chain与EasyExcel导入陷阱.md` 知识库条目（GOAL-103踩坑实战）。动机：Agent 容易忽视业务无关的代码模式问题，纯检查 Agent 无需理解业务，只对照规则做模式匹配
- 2026-06-30：dream 001 P13 补充设计 — 分点状态标注从单维度扩展为多维度标签体系（`【代码:*】【SQL:*】【部署:*】【配置:*】【验证:*】【数据:*】`），新增全局标签 `【开发中】【待确认】【阻塞】`；新增 GOAL 级执行仪表盘（Agent 自动维护的交付状态矩阵）；WITH-Finish 升级 v1.2（步骤4增加多维度完成检查+仪表盘更新）；目标总览 GOAL-00N 模板展示多维度标签；DREAM001-P13 改造草案新增第7章「多维度交付状态管理」（文本阶段方案+桌面程序数据模型演进+解析器升级注意事项）。更新 AS-TL.md v2.1（分点状态标注+执行仪表盘章节） 目标交互协议新增例外：TL 可在分点行追加/更新【状态标签】（【开发中】【已生成】【已提交】【待验证】【待确认】【阻塞】）。动机：人同时与多个窗口对话，状态标签让人类一眼看清进度。AS-TL.md 新增「分点状态标注」章节（10步流程新增第10步），目标总览模板 GOAL-00N 增加【】占位符。更新 CLAUDE.md 交互边界表
- 2026-06-29：需求收件箱双向化 + `/withOutbox` — 新建 `通用能力层/工具手册/UTIL-需求导出协议.md`（筛选非代码文件→去标识化→生成 README→放入收件箱）。新建 `/withOutbox` 指令（`WITH-Outbox.md`），与 `/withInbox` 形成入站/出站对称。去标识化规则：去除 GOAL 编号/分点号/ART 前缀/frontmatter，外部人员的 AI 可直接阅读不困惑。更新 `_需求收件箱/README.md` 为双向说明，CLAUDE.md 指令索引/决策树/导航/with系列描述，`🎛️_指令总览.md` 指令全表/触发规则/场景/文件索引，`_WITH-系列说明.md` 可用指令表，`AS-TL.md` 决策树
- 2026-06-29：dream 001 P4 需求收件箱 — 新建 `A.目标体系/_需求收件箱/`（外部需求统一入口，文件夹级别，含原型 HTML/文档等）。新建 `/withInbox` 指令（`WITH-Inbox.md`），实现收件箱处理管道：扫描→分析→建议归属→人确认→写入目标总览。新增场景7「需求收件箱处理」。更新 CLAUDE.md 决策树/导航/指令索引，`🎛️_指令总览.md` 指令全表/加载协议/场景，`_WITH-系列说明.md` 可用指令表，`AS-TL.md` 决策树
- 2026-06-26：CodeGraph 查询降级协议 — 全局 CLAUDE.md 和项目 CLAUDE.md 新增规则：CodeGraph 查询返回空时，先执行 `codegraph init -i` 重建索引再重试，仍为空才降级为 grep/Glob/Read。解决 Agent 在索引过期时直接跳入传统搜索的问题
- 2026-06-25：dream 001 p11 `/withGrill` v2.0 改造 — grill 输出从"直接修改目标总览"改为"独立 grill 明细文件 → 人答疑 → Agent 归集"。新增业务用户视角（与系统设计视角并列双视角提问）。门禁检查点从扫描目标总览改为扫描 grill 明细文件。归集格式参考 GOAL-002 结构化风格（不包含工时估算）。同步更新 `🎛️_指令总览.md`（场景2/4）和 `AS-TL.md`（grill 门禁描述）
- 2026-06-24：dream 001 p11 口语化查询条件生成 — 新建 `通用能力层/查询Schema注册表/`（页面查询条件结构化描述 + Agent 使用协议），首份 Schema：`fms/ARAP-统一应收付查询.yaml`（17个前端参数 + 10个后端参数 + 语义索引 + DB探查提示）。新建 `/withQuery` 指令（`WITH-Query.md`），实现口语→JSON 查询条件生成管道（解析口语→定位Schema→直接映射+DB探查→输出粘贴板JSON）。改造 `/withTest`（v1.1，测试场景矩阵增加 query_params_json 列）和 `/withMock`（v1.1，产物增加 JSON 查询条件文件，与 SQL 成对输出）。更新 CLAUDE.md 索引/决策树/导航
- 2026-06-24：dream 001 p10 Agent 反馈协议 — 新增 `UTIL-Agent反馈协议.md`，Agent 在 TL 模式下可通过"记下这个问题"等触发词将框架改进线索追加到相关框架文件底部 `## Agent 反馈` 区（一行一条，checkbox 格式）。`/withFinish` 末尾询问是否有反馈需要记录。Teacher 浏览框架文件时自然消费反馈，处理后勾选/删除。修改 AS-TL.md（决策树+行为约束）、WITH-Finish.md（六步扩展）、AS-Teacher.md（决策树+消费流程）、CLAUDE.md（决策树+导航策略） — 框架整体纳入 Git 版本控制（GitHub: GGFrame），创建 `.gitignore`（排除个人配置/Goal/Task/知识库/MCP密码），配置化改造（`项目注册表.template.yaml`/`框架设置.template.yaml`/`.mcp.json.template`），FW-FrameworkInit 新增 Phase 0「首次初始化检测」（git clone 后自动引导），框架设置增加 `git.update_url` 支持 Agent 自动检查更新。框架可安全通过 git 分享给团队成员，git pull 不会覆盖个人数据
- 2026-06-22：dream 001 p8 TL编写与finish能力增强 — 新增 `ROLE-QA-004` 代码质量检查员（轻量规范核验）、`CHK-项目代码质量自检`（Mapper/常量/异常/字典四维度）；AS-TL.md 新增「代码质量规范」章节（提交前必须派发质量检查）；`/withFinish` 完成协议从四步扩展为五步（新增步骤2代码质量检查）；`_封存/6.通用能力层/` 解封为顶层 `通用能力层/`（定位为离线 skill 库，TL 按需取用）；新增代码质量检查派发规则
- 2026-06-17：dream 001 p2 知识体系改造 — with 系列新增 `/withFinish` 指令（完成归一化：知识归集+代码提交+工作记录更新）；新建 `配置与元数据/框架设置.yaml`（知识库根路径可配置）；`知识沉淀/AGENT.md` 新增通识级收件箱处理协议（未整理信息→结构化知识的 B 方案管道）
- 2026-06-17：梦想改造方案 + dream 指令 — `A.Teacher分析/梦想改造方案.md` 改造为 DREAM-XXX 目标化结构（人类维护），新增 `圆梦记录.md`（Agent 维护）。fw 系列新增 `dream` 指令（`FW-Dream.md`），轻松对话式框架改造讨论，鼓励发散思维和主动建议更优方案
- 2026-06-15：mgmt 系列管理指令 + fw 系列独立 — 新增 `mgmt系列/`（管理分析指令：日报/周报/月报生成 `MGMT-Report.md`、工时估算汇总 `MGMT-Estimate.md`、用户自定义模板 `MGMT-模板.md`），双模式通用。fw 系列从 `as系列/` 独立为 `fw系列/` 目录，新增 `_FW-系列说明.md`
- 2026-06-12：Goal 为中心目录结构改造 — 框架从"任务系统+产物工件双中心"演进为"Goal 单中心"。每个 Goal 自包含文件夹（历史分点+工作记录+关联任务+产物），任务系统精简为模板+规范+控制台，A.产物工件/保留重定向。详见 `A.Teacher分析/改造方案-Goal为中心目录结构.md`
- 2026-06-12：fw 系列框架管理命令 — 新增 `/frameworkInit`（框架初始化引导，引导用户配置项目路径并更新项目注册表）和 `/frameworkClean`（框架重置，清理目标/任务/产物工件），两个命令均仅 Teacher 模式可用。新增 `FW-FrameworkInit.md`、`FW-FrameworkClean.md` 两个命令定义文件
- 2026-06-03：目标体系 — 新增 `A.目标体系/目标.md`（单文件，人类维护），极简设计：目标列表 + checkbox 阶段 + 关联任务。新增「目标交互协议」约束 AI 只读不写。TL 执行流程增加"先读目标"步骤
- 2026-06-08：新增 `/withReview` 指令 — 派发 subAgent 审核实施计划（完整性/可行性/边界遗漏/风险评估/架构一致性）。Goal 体系支持 `with -` 内联提示，TL 读取目标时自动检测并建议启用对应 with 指令
- 2026-06-01：模式指令子系统 + with 系列 — 新增 `角色体系/模式指令/` 子系统，将 as/with 系列指令从 CLAUDE.md 中抽离为独立文件实现渐进式加载；新增 `/withPlan`（强制计划先行）、`/withTest`（派发测试计划生成）、`/withMock`（派发测试数据 SQL 生成）三个 with 指令；CLAUDE.md 工作模式章节从 ~200 行缩减为 ~60 行索引表
- 2026-05-24：初始版本，基于 TASK-2026-001 全部 Phase 完成后的框架状态
- 2026-07-02：SQL 脚本产出路径调整 — SQL 脚本（DDL/菜单/数据迁移等）从产物工件系统移出，视为代码类产物直接写入项目 SQL 目录。项目注册表新增 `sql_dir` 字段（如 `fms-sql/`）。SQL 文件命名规范：`V{YYYYMMDD}_{动作}_{模块名/表名}.sql`（动作: `add`/`mod`/`del`）。更新 `配置与元数据/产物工件规范.md`（强制总则+DDL规则+Agent产出规则）、`AS-TL.md`（subAgent输出交付表+代码vs文档路径表）、`CLAUDE.md`（subAgent输出交付表）、`配置与元数据/项目注册表.yaml`（fms-rear/lasen-rear 新增 sql_dir）
