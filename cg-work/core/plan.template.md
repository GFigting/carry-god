# <任务名称>实施计划

## 目标

用一句话说明本任务交付的结果。

## 范围

### 包含

- 明确本次要改变的业务行为、模块或文档。

### 不包含

- 明确不会在本次任务中处理的相邻需求。

## 需求与验收

- 需求来源：`<requirements_reference>`
- 验收标准：
  - [ ] `<可观察的验收条件>`

## 影响范围

- 文件、模块、接口或数据：`<path-or-module>`
- 调用方和依赖方：`<consumer-or-dependency>`

## 实施步骤

1. `<先决条件或设计动作>`
2. `<实现动作>`
3. `<验证与交付动作>`

## 计划审核

适用于跨模块、高风险或用户明确要求审查的任务；简单任务可保留结论而不展开。

- 需求覆盖：`<完整 / 有缺口及处理方式>`
- 范围边界：`<无越界 / 越界项及处理方式>`
- 依赖与顺序：`<依赖、阻塞和可并行项>`
- 验收与验证：`<验收条件是否可观察，命令或环境是否明确>`
- 风险与回滚：`<主要风险、环境限制和回滚方式>`
- 审核结论：`<通过 / 修改后通过 / 暂停>`
- 审核意见来源：`<主 Agent 自审、子智能体意见或用户决定>`

## 验证

- 自动化测试：`<command>`
- 静态检查或构建：`<command>`
- 页面、接口或用户验收：`<具体动作>`

## 风险与交付

- 风险和环境限制：`<risk-or-limitation>`
- 集成方式：`<保留、合并、推送或暂缓>`
