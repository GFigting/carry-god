# 计划模板与审核清单实施计划

## 目标

提供一份可复用的标准任务计划骨架，降低计划遗漏范围、依赖、验收和验证信息的风险。

## 计划审核

- 需求覆盖、范围边界、依赖、步骤、验收标准和风险均有位置记录。
- 计划审核直接作为 `plan.md` 的章节，不增加新的产物类型。

## 验证

- 运行 `npm test`。
- 运行本任务的 `check-task.mjs`。
- 运行 `git diff --check`。
