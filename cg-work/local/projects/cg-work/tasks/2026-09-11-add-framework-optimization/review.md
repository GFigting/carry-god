# 审查记录

## 审查范围

审查新增的 `workflows/framework-optimization.md` 与 `skills/framework-optimization/SKILL.md`，以及其索引、维护规则和版本同步。

## 结论

未发现阻塞问题。

- 工作流只处理 cg-work 自身的演进，明确排除项目业务代码重构，避免与 `refactor.md` 重叠。
- 工作流引用的所有必需和可选技能都已存在；`check-all` 已验证前置 YAML、引用和新技能目录。
- 新技能是框架自有技能，不进入镜像映射；技能索引明确该分类。
- 兼容性分级、迁移/替代说明、唯一来源、数据隔离和验证门禁都已写入技能正文。
- 新增兼容能力按维护规则从 1.5.0 升级为 1.6.0。

## 遗留项

`node scripts/check-skills.mjs` 报告多项既有镜像与上游源内容不一致；本次未修改任何镜像，且失败项不包含新增框架自有技能。镜像同步应作为独立框架优化任务处理。
