---
type: capability
id: CGHN-CAP-002
stage: design
---

# 深模块设计

## 目标

让模块通过小而稳定的接口隐藏实现复杂度，降低变更传播。

## 检查

- 接口是否表达真正的业务动作，而不是暴露内部步骤？
- 模块是否拥有完整且一致的责任？
- 调用方是否只依赖必要信息？
- 失败、重试、幂等和边界输入是否有明确行为？
- 测试是否能通过接口验证，而不是绑定内部实现？

## 输出

边界图、接口契约、关键数据流、错误策略、兼容性影响和架构决策记录。
